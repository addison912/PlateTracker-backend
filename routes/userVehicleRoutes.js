const express = require("express"),
  router = express.Router(),
  userVehicleController = require("../controllers/userVehicle.js");

module.exports = router;
// .get("/", userVehicleController.index)
// .get("/:id", userVehicleController.index)
// .post("/", userVehicleController.post)
// .put("/:id", userVehicleController.edit);
